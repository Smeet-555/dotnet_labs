// Create a class BankTransaction with overloaded Transfer() methods to
// transfer money between two accounts (using amount only) and between
// two accounts with an additional message (using amount and description).
// Demonstrate method overloading and use private fields with public access
// methods.